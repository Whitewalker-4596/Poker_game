here i am going to build poker game using python Test driven development 

TODO:
add edge cases in three of a kind
add edge case in straight and straight flush when there is pair included.
add bets concept and call it after each round
add logic when both players have same hand like in actual poker
we can build web app and add interactions like call,fold,raise,check
add the concept of button


command to run all tests in the tests folder:
python -m unittest discover <folder name>
python -m unittest discover tests

command to run single test file in the tests folder:
python -m unittest  <file path>
python -m unittest  tests/test_card.py

instansiated the objects required to run the game in main.py file 
add if anything you want to see in output in the main.py file

just run the game by running the main.py 


